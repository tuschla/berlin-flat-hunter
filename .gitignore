__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.coverage
htmlcov/
config.yaml
# Real profile configs hold Telegram tokens + landlord-portal credentials.
# Commit a sanitized template separately if you want one in git.
profiles/*.yaml
profiles/*.json
/profile.json
# Orchestrator config: lists profiles + the shared alert Telegram token.
hunter.yaml
*.db
*.sqlite
schema_monitor.json
# Runtime per-profile state: stats DBs (+ sqlite -wal/-shm sidecars) and
# schema-monitor snapshots. Never code — keep the whole tree out of git.
data/
.claude/
